table:
  name: passwords
  schema: public
select_permissions:
  - role: user
    permission:
      columns:
        - attempts
        - hash
        - iterations
        - salt
      filter:
        userId:
          _eq: X-Hasura-User-Id
