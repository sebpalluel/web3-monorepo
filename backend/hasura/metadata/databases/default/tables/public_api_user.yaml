table:
  schema: public
  name: api_user
event_triggers:
  - name: new_registration_email
    definition:
      enable_manual: false
      insert:
        columns: '*'
    retry_conf:
      num_retries: 0
      interval_sec: 10
      timeout_sec: 60
    webhook: http://localhost:8000/api/logic/new_registration_email/
