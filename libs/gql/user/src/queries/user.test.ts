// import { userClient } from '@governance/test-utils-gql';

describe('user access security tests', () => {
  beforeAll(async () => {
    // const client = await userClient();
    // // // ️️️✅ Best Practice: Clean-up resources after each run
    // await client.deleteUser({ id: '1' });
    // seed the database with two users alpha and beta
  });
  // afterAll(async () => {});
  // it('user alpha can retreive his information', async () => {});
});
