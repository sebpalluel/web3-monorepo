# test-utils-gql

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build test-utils-gql` to build the library.

## Running unit tests

Run `nx test test-utils-gql` to execute the unit tests via [Jest](https://jestjs.io).
