# arbitrum

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build arbitrum` to build the library.

## Running unit tests

Run `nx test arbitrum` to execute the unit tests via [Jest](https://jestjs.io).
