import { render } from '@testing-library/react';

import { useDid } from './service-did';

describe('ServiceDid', () => {
  it.skip('should render successfully', () => {
    // const { baseElement } = render(<DidProvider />);
    // expect(baseElement).toBeTruthy();
  });
});
